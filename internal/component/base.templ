package component

templ Base(alpinejsUrl, htmxUrl string) {
    <html>
        <head>
            <script defer src={alpinejsUrl}></script>
            <script src={htmxUrl}></script>
        </head>
        <body>
            {children...}
        </body>
    </html>
}

templ Home() {
    <h1 x-data="{ message: 'I ❤️ Alpine' }" x-text="message"></h1>
}

templ Park() {
    <h1 x-data="{ message: 'Hello' }" x-text="message"></h1>
}